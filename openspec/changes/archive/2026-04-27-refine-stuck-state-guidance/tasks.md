## 1. UI Refinement (popup.js)

- [x] 1.1 Update the `stuck` state message in `updateUI` to the more nuanced version.
- [x] 1.2 Ensure the color and formatting match the "Stalled" status design.

## 2. Verification

- [x] 2.1 Trigger a stuck state (by letting the extraction finish or pausing Teams) and verify the new message is displayed correctly.
- [x] 2.2 Confirm that the instructions are clear and that all mentioned buttons (Resume Manually, Stop and Export) are visible.
