## 1. UI Modification (popup.html)

- [x] 1.1 Remove the "Currently loaded messages" button (`data-days="-1"`) from `popup.html`.
- [x] 1.2 Remove the "Last 24 hours" button (`data-days="1"`) from `popup.html`.

## 2. Verification

- [x] 2.1 Open the extension popup and verify that "Currently loaded messages" and "Last 24 hours" are no longer present in the time range options.
- [x] 2.2 Perform an extraction using one of the remaining options (e.g., "Last 7 days") to ensure the UI remains functional.
