## 1. UI Styling (popup.html)

- [x] 1.1 Add global CSS rule for `button:disabled` to provide consistent visual feedback.

## 2. Logic Updates (popup.js)

- [x] 2.1 Implement reset logic for the download button when returning to the initial state.

## 3. Verification

- [x] 3.1 Verify that all buttons show the correct disabled style when inactive.
- [x] 3.2 Verify that the download button correctly resets when starting a new extraction.
