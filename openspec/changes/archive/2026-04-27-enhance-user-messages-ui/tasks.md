## 1. UI Refinement (popup.html / popup.js)

- [x] 1.1 Update the `stuck` state template in `updateUI` to use the standardized wording.
- [x] 1.2 Update the `extracting` state (resumed) message to "Extraction resumed successfully!".
- [x] 1.3 Move `#disclaimerBox` in `popup.html` so it is outside the `#status` div and always visible.
- [x] 1.4 Update the text in `#disclaimerBox` to include the window minimization warning.
- [x] 1.5 Remove visibility management for `#disclaimerBox` from `popup.js` (since it's now always visible).
- [x] 1.6 Ensure formatting (spacing/alignment) is consistent between all alerts.

## 2. Verification

- [x] 2.1 Verify the "Stalled" message provides the correct instructions and looks professional.
- [x] 2.2 Verify the "Resumed" message is clearly visible and rewarding.
