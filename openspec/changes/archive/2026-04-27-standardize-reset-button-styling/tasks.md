## 1. Style Standardization (popup.html)

- [x] 1.1 Remove inline styling from `#resetBtn` in `popup.html`.
- [x] 1.2 Define a unified button style in the `<style>` block of `popup.html` or apply existing classes to `#resetBtn`.
- [x] 1.3 Ensure the `#resetBtn` background, padding, and border-radius match the primary buttons (e.g., time-range buttons and download button).
- [x] 1.4 Add hover and active transition effects to `#resetBtn`.

## 2. Verification

- [x] 2.1 Verify that the "Start New Extraction" button now looks consistent with the "Download Archive" button in terms of shape, color, and interaction feedback.
