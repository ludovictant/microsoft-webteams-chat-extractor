## 1. Extraction (payload.js)

- [x] 1.1 Update `serializeMessage` to find and extract reaction summaries.
- [x] 1.2 Implement logic to parse emojis and counts from reaction pills.

## 2. Rendering (background.js)

- [x] 2.1 Update `renderHTML` to display reactions in a "pill" style below the message body.
- [x] 2.2 Update `renderMarkdown` to append a reaction summary to each message.
- [x] 2.3 Update `renderCSV` to include a "Reactions" column.

## 3. Verification

- [x] 3.1 Verify that reactions are correctly extracted from both private chats and channels.
- [x] 3.2 Ensure reactions are properly styled in the HTML export.
- [x] 3.3 Confirm the text summary in Markdown and CSV is accurate.
