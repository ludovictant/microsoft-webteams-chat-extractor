## 1. UI Updates (popup.js)

- [x] 1.1 Update `updateUI` to use "Stop and Export" in the `extracting` state.
- [x] 1.2 Update `updateUI` to use "Stop and Export" in the `stuck` state.
- [x] 1.3 Update `updateUI` to use "Stop and Export" in the `processing` state.

## 2. Text Alignment (popup.html)

- [x] 2.1 Update the text in `#disclaimerBox` to reference "Stop and Export" instead of "Stop Extraction then Export".

## 3. Verification

- [x] 3.1 Verify the button text is consistent across all active extraction phases.
- [x] 3.2 Confirm the disclaimer text matches the button label.
